// src/app/location-picker.tsx
// Root-level alias so authenticated screens (profile, personal-details, etc.)
// can navigate to the map picker without touching the (auth) group.
export { default } from "./(auth)/location-picker";
export { unstable_settings } from "./(auth)/location-picker";
